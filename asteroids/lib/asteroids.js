const MovingObject = require('./moving_object.js');
window.MovingObject = MovingObject;

const Util = require('./utils.js');
window.Util = Util;
// let bob = MovingObject({ pos: [30, 30], vel: [10, 10], radius: 5, color: "#00FF00"});
// console.log(bob);

// bob.draw(whereIsTheContext)
